[project]
name = "pygrep"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
pygrep = "pygrep.main:main"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
lint = [
    "ruff>=0.15.12",
]
